"""Unit tests for prompt-injection detection.

Placeholder — the injection layer is implemented in Milestone M7. These tests
are written then (regex Layer-1 corpus + mocked LLM Layer-2). Kept as a skipped
stub so the test module exists per the "test file per module" standard.
"""

import pytest

pytestmark = pytest.mark.skip(reason="Injection layer implemented in Milestone M7")


def test_injection_detection_placeholder():
    raise NotImplementedError
